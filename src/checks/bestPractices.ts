import * as cheerio from "cheerio";

export async function checkBestPractices($: cheerio.Root, url: string): Promise<string[]> {
    const issues: string[] = [];

    // Tutaj można dodać nową logikę sprawdzania najlepszych praktyk

    return issues;
}